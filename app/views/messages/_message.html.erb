<% own = message.user_id == current_user.id %>

<div id="<%= dom_id(message) %>"
     class="message-wrapper <%= own ? 'message-right' : 'message-left' %>">

  <% unless own %>
    <div class="message-avatar">
      <%= image_tag "https://ui-avatars.com/api/?name=#{CGI.escape(message.user.name)}",
            class: "rounded-circle",
            style: "width: 36px; height: 36px; object-fit: cover;" %>
    </div>
  <% end %>

  <div class="message-bubble <%= own ? 'my-bubble' : 'other-bubble' %> shadow-sm">

    <%# ============ NOMBRE DE USUARIO (O TÚ) ================= %>
    <div class="<%= ['message-username', ( "user-name-#{message.user_id % 5 + 1}" unless own )].compact.join(' ') %>">
      <strong><%= own ? "Tú" : message.user.name %></strong>
    </div>

    <%# ============ CONTENIDO DEL MENSAJE ==================== %>
    <% if message.content.present? %>
      <p class="mb-1 message-text"><%= message.content %></p>
    <% end %>

    <%# ============ ARCHIVOS ADJUNTOS ======================== %>
    <% if message.files.attached? %>
      <div class="mt-2">
        <% message.files.each do |file| %>

          <% if file.image? %>
            <%= link_to url_for(file), target: :_blank do %>
              <%= image_tag file.variant(resize_to_limit: [300, 300]).processed,
                    class: "img-thumbnail mb-2",
                    style: "max-width: 150px; height: auto; border-radius: 8px;" %>
            <% end %>
          <% else %>
            <div class="mb-1">
              📎 <%= link_to file.filename.to_s, url_for(file), target: :_blank %>
            </div>
          <% end %>

        <% end %>
      </div>
    <% end %>

    <%# ============ TIMESTAMP ================================ %>
    <div class="small timestamp <%= own ? 'text-light' : 'text-muted' %>">
      <%= message.created_at.strftime("%d/%m/%Y %H:%M") %>
    </div>

    <%# ============ BOTÓN ELIMINAR =========================== %>
    <% if current_user.administrator.present? || own %>
      <div class="mt-1">
        <%= link_to "Eliminar",
          chat_message_path(message.chat, message),
          data: {
            turbo_method: :delete,
            turbo_confirm: "¿Seguro que querés eliminar este mensaje?"
          },
          class: "delete-message-btn" %>
      </div>
    <% end %>

  </div>

  <% if own %>
    <div class="message-avatar">
      <%= image_tag "https://ui-avatars.com/api/?name=Tú",
            class: "rounded-circle",
            style: "width: 36px; height: 36px; object-fit: cover;" %>
    </div>
  <% end %>

</div>
